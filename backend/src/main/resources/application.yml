server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_management_sys?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 7355608
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jackson:
    time-zone: Asia/Shanghai

  config:
    import: optional:configtree:./secrets/
mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:mapper/*.xml

jwt:
  secret: change-this-secret-to-32-bytes-key-123
  expiration-minutes: 120000

